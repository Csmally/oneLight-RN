/**
 * APP版本号
 */
const appVersion = {
  major: 1,
  minor: 0,
  patch: 0,
};

/**
 * API版本号
 */
const apiVersion = {
  major: 1,
  minor: 0,
  patch: 0,
};

/**
 * 客户端类型
 */
const clientTypes = {
  ios: 1,
  android: 2,
  web: 3,
};

const DeviceInfo = {
  BASE_WIN_WIDTH: 375,
  BASE_WIN_HEIGHT: 667,
  DEFAULT_BOTTOM_SAFEAREA: 50, // 默认底部安全距离
};

export { appVersion, apiVersion, DeviceInfo, clientTypes };
